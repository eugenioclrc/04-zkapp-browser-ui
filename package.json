{
  "name": "ui",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd iu && next dev",
    "build": "cd iu && next build",
    "start": "cd iu && next start",
    "lint": "cd iu && next lint",
    "ts-watch": "cd iu && tsc --noEmit --incremental --watch",
    "export": "cd iu && next export",
    "deploy": "cd iu && next build && next export && touch out/.nojekyll  && git add -f out && git commit -m \"Deploy gh-pages\" && cd .. && git subtree push --prefix ui/out origin gh-pages"
  }
}
